# PR8 Credential Resume

The preflight reports only missing credential capabilities. It never prints tokens or client secrets. Until an independent TEST account exists it returns `PR7_TEST_IDENTITY_REQUIRED` normally. Live provisioning remains blocked.
