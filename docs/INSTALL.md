# INSTALL

## Local

1. 解壓 ZIP。
2. 覆蓋目前 Repository。
3. 開啟 `index.html` 測試畫面。
4. 先不要填 `API_BASE` 也可以測 UI，送出時會提示 API 尚未設定。

## Production

1. 完成 Apps Script 部署。
2. 修改 `js/config.js`。
3. Commit / Push。
4. 到 GitHub Pages 測試 Waiting List。
