# PR7 CI Result

The PR Validation workflow runs syntax, operations, polling, adapter integration, runtime emulator, deterministic browser harness, Stage 2, Automation, required files, RC6, diff hygiene, and sensitive scanning. It has read-only repository permission and contains no deployment step.

Local results must not be represented as the GitHub-hosted workflow result until the branch commit is pushed and GitHub reports success.
