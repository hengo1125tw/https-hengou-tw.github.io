# PR7 Audit Automation

The PR7 operations suite covers duplicate identifiers, saved rows without valid requestIds, stale processing based on `processing_started_at`, invalid timestamps, notification metadata states, stale/invalid claims, and Sheet boolean normalization. Audit findings never repair Gmail automatically under the at-most-once contract.
